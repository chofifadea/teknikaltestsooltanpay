sudo apt-get install composer -y
sudo add-apt-repository ppa:ondrej/php -y
sudo apt-get install nginx php7.4 php7.4-mysql php7.4-mbstring php7.4-xml php7.4-zip php7.4-curl git unzip -y

sudo mysql -u root -p << EOF 
create user 'chofifa'@'localhost' identified by '1234567890';
grant all privileges on *.* to 'chofifa'@'localhost';
\q
EOF

mysql -u chofifa -p1234567890 << EOF
create database blog;
\q
EOF


sudo chmod 755 /usr/share/nginx/html
sudo chown $(whoami):$(whoami) /usr/share/nginx/html
cd /usr/share/nginx/html
sudo git clone https://github.com/happyduck303/Laravel-8-CRUD
sudo mv Laravel-8-CRUD/* .
sudo mv Laravel-8-CRUD/.git .
sudo mv Laravel-8-CRUD/.gitignore
sudo mv Laravel-8-CRUD/.env.example .
sudp cp .env.example .env #ubah sesui nama db,user dan password
sudo php artisan migrate
sudo php artisan key:generate
sudo php artisan serve
